// Este código esperará a que la página se haya cargado y luego enviará el formulario
/* document.addEventListener('DOMContentLoaded', function() { */
    // Solo envía el formulario una vez
/*     if (!document.getElementById('consulta').classList.contains('enviado')) {
      document.getElementById('consulta').submit();
      document.getElementById('consulta').classList.add('enviado');
    }
  }); */